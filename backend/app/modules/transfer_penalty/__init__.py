"""Transfer penalty module: line-pair surcharge rules persisted in SQLite.

Quote-time transfer counting lives in ``engine``; the table and its CRUD in
``repository``/``service``; HTTP endpoints in ``router``.
"""
